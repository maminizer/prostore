import Link from "next/link";
import Image from "next/image";
import { Card } from "@/components/ui/card";

const ProductCard = ({ product }: { product: any }) => {
  return <Card className="w-full max-w-sm"></Card>;
};

export default ProductCard;
